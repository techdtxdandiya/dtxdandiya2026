{
  "rules": {
    "teams": {
      "$teamId": {
        // Allow read access to existing team data
        ".read": true,
        // Allow write access to admin
        ".write": "auth != null && auth.token.admin === true"
      }
    }
  }
} 